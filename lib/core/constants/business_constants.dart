class BusinessConstants {
  const BusinessConstants._();

  static const warningAfter = Duration(minutes: 35);
  static const alarmAfter = Duration(minutes: 40);
  static const maxActiveVehicles = 100;
}
